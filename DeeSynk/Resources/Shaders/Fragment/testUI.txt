#version 450 core

//in vec2 vs_texCoord;
in vec4 vs_color;
in vec4 vs_pos;
uniform sampler2D tex;
out vec4 color;

void main(void)
{
	//color = vs_color;
	//color = vec4(gl_FragCoord.x/2560, gl_FragCoord.y/1440, 0, 1);
	if(vs_pos.x > 0)
		color = vec4(1);
	else
		color = vec4(0);
	//color = vec4(vs_pos.x, vs_pos.y, 0, 1);
}